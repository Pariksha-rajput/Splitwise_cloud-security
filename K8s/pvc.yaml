apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fairsplit-db-pvc
  namespace: fairsplit
spec:
  accessModes:
    - ReadWriteOnce       # Azure Disk — one pod at a time
  storageClassName: managed-csi
  resources:
    requests:
      storage: 1Gi
