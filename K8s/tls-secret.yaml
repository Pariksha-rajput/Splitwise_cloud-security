#Need to generate TLS certificate in cli

apiVersion: v1
kind: Secret
metadata:
  name: tls-secret
  namespace: splitwise
type: kubernetes.io/tls
data:
  tls.crt: <base64-encoded-cert>
  tls.key: <base64-encoded-key>